Facial-Detection-Software-in-Python

If you are going to use this with a built in webcam on a laptop, change the value of the cv.VideoCapture to 0 instead of 1
Also make sure you install and set up the OpenCV Python library so the commands work.